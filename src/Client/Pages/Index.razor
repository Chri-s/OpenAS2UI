@page "/"
@inject NavigationManager NavigationManager

<PageTitle>OpenAS2</PageTitle>

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();

        NavigationManager.NavigateTo("/dashboard/", false, true);
    }
}